name: Backlog Labeller
description: Automatically manages the backlog label for issues
inputs:
  token:
    description: GitHub API token
    required: true
runs:
  using: docker
  image: docker://docker.io/nacx/backlog-labeller:1.0
  args:
    - '-token'
    - ${{ inputs.token }}
